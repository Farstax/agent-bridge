Synthetic event sequences shaped like the provider probes in issue #897
(field names and ordering only; no provider payloads, credentials or session data).
They are not live captures. Live qualification traces replace them per provider.
